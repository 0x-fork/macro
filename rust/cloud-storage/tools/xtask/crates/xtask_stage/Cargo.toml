[package]
description = "xtask stage: shared staged-progress terminal UI for xtask command crates"
edition = "2024"
name = "xtask_stage"
publish = false
version = "0.1.0"

[dependencies]
anyhow = { workspace = true }
console = "0.15"
indicatif = "0.18"
workspace-hack = { version = "0.1", path = "../../../../workspace-hack" }
