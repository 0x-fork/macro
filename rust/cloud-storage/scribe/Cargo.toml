[package]
edition = "2024"
name = "scribe"
publish = false
version = "0.1.0"

[features]

all = ["channel", "dcs", "document", "email", "static_file"]
channel = ["comms_db_client", "models_comms", "dep:sqlx"]
dcs = ["dep:chat", "macro_db_client", "dep:sqlx"]
default = []
document = [
  "document_storage_service_client",
  "lexical_client",
  "sync_service_client",
  "properties_db_client",
  "models_properties",
  "macro_db_client",
  "macro_user_id",
  "models_permissions",
  "dep:sqlx",
  # TODO
  # "static_file_service_client??",
]
email = ["email_service_client", "models_email"]
static_file = ["models_sfs", "static_file_service_client"]

[dependencies]
# Core dependencies
anyhow = { workspace = true }
bytes = { workspace = true }
chrono = { workspace = true }
reqwest = { workspace = true }
serde_json = { workspace = true }
sqlx = { workspace = true, optional = true }
tracing = { workspace = true }
uuid = { workspace = true }

# Internal dependencies
ai = { path = "../ai" }
ai_format = { path = "../ai_format" }
model = { path = "../model" }

# Service client dependencies
document_storage_service_client = { path = "../document_storage_service_client", optional = true }
lexical_client = { path = "../lexical_client", optional = true }
sync_service_client = { path = "../sync_service_client", optional = true }

chat = { path = "../chat", optional = true, default-features = false, features = ["outbound"] }
comms_db_client = { path = "../comms_db_client", optional = true }
macro_db_client = { path = "../macro_db_client", optional = true }
email_service_client = { path = "../email_service_client", optional = true }
macro_user_id = { path = "../macro_user_id", optional = true }
models_comms = { path = "../models_comms", optional = true }
models_permissions = { path = "../models_permissions", optional = true }
models_email = { path = "../models_email", optional = true }
models_properties = { path = "../models_properties", optional = true }
models_sfs = { path = "../models_sfs", optional = true }
properties_db_client = { path = "../properties_db_client", optional = true }
static_file_service_client = { path = "../static_file_service_client", optional = true }
