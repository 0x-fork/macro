@macro-inc:registry=https://npm.pkg.github.com
always-auth=true

# Use a GitHub token with `read:packages` (and SSO enabled if your org requires it).
# Export it before installing:
#   export GITHUB_TOKEN=...
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}



